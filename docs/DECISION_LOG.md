# Decision Log

A chronological log of all technical, AI, and product decisions made during development. From this point forward, every schema, AI-call, or product decision goes here instead of living only in a chat transcript.

| Date | Decision | Options Considered | What Was Chosen | Why |
| ---- | -------- | ------------------ | --------------- | --- |
| Aug 31, 2026 | **Correction 1: Payment Processing Scope** | 1) Full Stripe/Razorpay processing. 2) Display-only UPI/Link. | **Display-only.** | The real product spec explicitly shows a UPI ID, payment link, and QR upload, but no real processing backend. Processing was removed to match the scope of Marg as a proposal builder, not a payment gateway. |
| Aug 31, 2026 | **Correction 2: Settings Rebuild** | 1) Keep API Keys & Notifications tabs. 2) Stick to the 5-tab real spec. | **Stick to 5-tab spec.** | "Notifications" is a separate screen in the design system, and "API keys" was completely invented. Roles (owner, approver, drafter) were wired into the real approval chain. |
| Aug 31, 2026 | **Correction 3: Onboarding Flow** | 1) Dashboard-inline checklist. 2) Full 4-step wizard. | **4-step wizard.** | The true design source used an immersive, step-by-step wizard capturing business categories and extracting brand kits before entering the dashboard. |
| Aug 31, 2026 | **Correction 4: Brand Extraction Sources** | 1) Animate all 5 sources with fake results. 2) Mark unimplemented ones "Coming Soon". | **Mark unimplemented ones "Coming Soon".** | Being honest about real functionality. URL and image extraction run real API backends. Brand guide, codebase, and describe were deferred honestly. |
| Aug 31, 2026 | **Correction 5: Accept & Sign Flow** | 1) Dummy accept button. 2) Implement SignaturePad & DealWon. | **Implement SignaturePad & DealWon.** | Match the exact visual finish of the recipient client view, recording actual `accepted_by_name` and `accepted_at` timestamps. |
| Sep 1, 2026 | **Correction 6.1 & 6.2: Dashboard/Templates** | 1) Keep hardcoded 6-industry categories. 2) Use real data. | **Use real data.** | Category chips map dynamically to the actual `category` column of existing template rows. "Use template" and "Start blank" wire to real routing state. |
| Sep 1, 2026 | **Correction 6.3: AI Proposal Generation** | 1) Fake scripted regex UI from mockup. 2) Real streaming GPT-4o backend mapped to the UI. | **Real streaming GPT-4o backend mapped to the UI.** | Porting the fake regex AI would regress the platform's core differentiator. Mapped the visual UI onto the working AI backend. *Added a `preview` object to the AI tool schema* to populate the confirm modal honestly. |
| Sep 1, 2026 | **Product Decision: Currency Handling** (planned, not yet built — see M4) | 1) Hardcode to INR. 2) Currency agnostic. 3) Built-in currency switching. | **Built-in currency switching.** | To support a broader global market from day one, allowing users to select USD, EUR, INR, etc. *Decided via a `/grill-me` interview — option drafted by Anti-Gravity, selected by Sahil.* |
| Sep 1, 2026 | **Product Decision: E-Signature Compliance** | 1) Basic Name+Timestamp. 2) Full legal compliance (PDF stamping, audit logs). | **Basic Name+Timestamp.** | Keeps MVP scope tight. Advanced compliance features are deferred. *Decided via a `/grill-me` interview — option drafted by Anti-Gravity, selected by Sahil.* |
| Sep 1, 2026 | **Product Decision: SaaS Billing Provider** (planned, not yet built — see M4) | 1) Stripe. 2) Free MVP. 3) Alternative providers (Razorpay/LemonSqueezy). | **Alternative providers.** ⚠️ *Superseded — see Sep 2, 2026 entry below.* | Will design the billing architecture to accommodate Razorpay/LemonSqueezy for SaaS subscriptions. *Decided via a `/grill-me` interview — option drafted by Anti-Gravity, selected by Sahil.* |
| Sep 1, 2026 | **Product Decision: Team Seat Billing** | 1) Included in plan tier. 2) Billed dynamically per extra seat. | **Included in plan tier.** | Simplifies billing logic for the MVP; e.g., the Agency plan simply unlocks a higher seat limit. *Decided via a `/grill-me` interview — option drafted by Anti-Gravity, selected by Sahil.* |
| Sep 1, 2026 | **Product Decision: Editor Autosave UX** | 1) Debounced (2-3s). 2) Lazy (on blur). 3) Manual save button. | **Debounced autosave (2-3s).** | Provides a modern, Google Docs-like experience without overwhelming the database with updates on every keystroke. *Decided via a `/grill-me` interview — option drafted by Anti-Gravity, selected by Sahil.* |
| Sep 2, 2026 | **Correction: SaaS Billing Provider supersedes Sep 1 entry** | 1) Keep Razorpay/LemonSqueezy as recorded. 2) Move to Stripe per `docs/MVP_LAUNCH_PLAN.md` §3.1(a). | **Stripe.** ⚠️ *Superseded — see Sep 4, 2026 entry below.* | `docs/MVP_LAUNCH_PLAN.md` §3.1(a) explicitly superseded the Sep 1 entry in favor of Stripe (Stripe Managed Payments) before this was ever built, and the actual shipped code (`lib/stripe.ts`, `/api/billing/checkout`, `/api/webhooks/stripe`) already implements Stripe, not Razorpay. This row corrects the log to match reality rather than leaving a stale entry uncorrected. |
| Sep 4, 2026 | **Correction: SaaS Billing Provider reverts to a non-Stripe, India-friendly processor** | 1) Keep Stripe as recorded Sep 2. 2) Move to LemonSqueezy or another provider with easier India support. | **Moving away from Stripe** — exact replacement (LemonSqueezy vs. another option) not yet finalized. | Sahil decided against Stripe for India-market support reasons; told directly in conversation, not yet reflected in `docs/MVP_LAUNCH_PLAN.md` or the shipped code (`lib/stripe.ts`, `/api/billing/checkout`, `/api/webhooks/stripe` are all still Stripe-shaped). Billing-provider-specific work (master fix plan Phase 5, and the Stripe-Price-ID-dependent part of Phase 6's pricing-page fix) is paused until the replacement is chosen — no point fixing bugs in code that's about to be swapped out. |
| Sep 4, 2026 | **Evaluation: upgrade the AI SDK packages (`ai`, `@ai-sdk/openai`, `@ai-sdk/anthropic`)** | 1) Upgrade now to latest (`ai@7.x`, provider packages `@4.x`). 2) Defer as its own dedicated effort. | **Deferred — not upgraded in this pass.** | Master fix plan Phase 17 asked to *evaluate* upgrading, not necessarily do it. Current versions (`ai@3.2.36`, providers `@0.0.36`) are 3-4 major versions behind latest (`ai` went through breaking rewrites across v4/v5/v6/v7 — provider architecture, message/UI shapes, etc.). This session's Phase 7/8 work built directly on the current v3 API surface (`generateObject`/`generateText`/`streamText`/`tool()`) across all 9 real AI call sites in the app — migrating would mean rewriting and re-testing every one of them for uncertain, unscoped benefit right now, which is a real project of its own, not a housekeeping-sized task. Recorded here so the recommendation isn't silently dropped: worth doing as its own dedicated phase once there's a concrete reason (a needed v4+-only feature, or the v3 line going unsupported), not bundled into general cleanup. |
| Sep 8, 2026 | **Known gap: proposal-generation credits/usage have zero enforcement** | 1) Leave unenforced until a real billing provider is chosen (Stripe is being replaced — see Sep 4 entry above — so building metering against soon-to-be-discarded billing code is premature). 2) Build a minimal enforcement layer now, ahead of the billing-provider decision. | **Logged as a known, currently-real gap — not fixed by this entry, needs its own ticket once the billing provider is chosen.** | `credit_transactions` was created as scaffolding, then dropped (`20260901140000_drop_credit_transactions.sql`) before anything ever read or wrote it. Nothing replaced it: a live audit on 2026-09-08 grepped the entire `app/` and `lib/` tree for `credit`, `quota`, and `usage limit` and found zero enforcement code anywhere. The only real, code-enforced limit tied to `plan_tier` is custom-domain slot count (`agency`→3, `pay_per_proposal`→1, `free`→0, in `app/dashboard/settings/actions.ts`) — a different thing entirely. The free tier's own pricing copy claims "1 active proposal" (`app/dashboard/settings/SettingsClient.tsx`) and the `pay_per_proposal` plan's very name implies per-proposal metering — neither is backed by any check. A free-tier account can create unlimited proposals today with zero pushback. Not fixed here because building real metering against Stripe-shaped billing code that's already slated for replacement would be wasted work — but this needs to be built as part of (or immediately after) whichever billing provider is finally chosen, not forgotten. |
| Sep 8, 2026 | **Security incident: `.env.local` was at some point pointed directly at the production Supabase project** | 1) Leave unaddressed — service-role key exposure risk from local dev tooling pointed at prod stays open. 2) Rotate the production `SUPABASE_SERVICE_ROLE_KEY` and confirm `.env.local` now points only at local dev. | **Rotated and confirmed closed 2026-09-08.** | A 2026-09-08 forensic audit (triggered by an unexplained production row-count drop — see below) found `.env.local` currently contains a commented-out `OLD_NEXT_PUBLIC_SUPABASE_URL` pointing at the real production project (`qmnxkdmtiicdyneafban`), meaning it was pointed at production before being switched to local (`127.0.0.1:54321`) at some point. `scripts/create-test-user.js` (dated 2026-09-02) reads its Supabase URL and service-role key straight from `.env.local` — so whatever it was pointed at when that script ran had its service-role key used directly. Supabase's own `auth_audit_logs` confirm a real signup (`sahilbagul7641@gmail.com`, 2026-09-03T03:03:09Z) was performed via the Management API using `service_role`, not through the app's own signup page — consistent with exactly this. Separately, and likely related: production's `accounts`, `proposals`, `brand_kits`, `notifications`, `domains`, and `invitations` tables all show a hard timestamp cutoff around Sep 2-3 (everything from before is gone; `auth.users` and `public.users` are in perfect 1:1 sync with zero orphans, which is more consistent with a full database restore to an early backup than a targeted delete) — Sahil recalls doing "some kind of data migration" in this window but wasn't certain what. Whether or not that specific action is what happened, a service-role key that was ever reachable from a local `.env.local` file should be treated as potentially exposed.

**Remediation completed 2026-09-08:** `.env.local` reconfirmed pointed only at local Docker Supabase (`127.0.0.1:54321`), never production, both before and after this session's Vercel CLI operations. Production Vercel env vars migrated off the legacy shared-secret key format entirely — `NEXT_PUBLIC_SUPABASE_ANON_KEY` → new-format publishable key (safe to expose, unchanged since), `SUPABASE_SERVICE_ROLE_KEY` → new-format secret key *[value redacted — see standing rule below; this specific key was itself replaced in the second rotation and is now dead]*, redeployed, confirmed live via direct HTTP checks (homepage 200, a real proposal page returning its correct DB-backed title, login page clean). The old legacy keys were then fully disabled and the underlying Legacy HS256 JWT secret revoked in Supabase's dashboard. Confirmed dead by direct test, not assumption: a REST call to `qmnxkdmtiicdyneafban.supabase.co` using the old legacy `anon` JWT now returns `401` — `"Legacy API keys are disabled" / "Your legacy API keys (anon, service_role) were disabled on 2026-09-08T11:10:19.011419+00:00"` — a Supabase-server-issued timestamp, not a client-side or UI-only state. Production reconfirmed healthy on the new keys immediately after the revoke (homepage 200, same proposal page still resolving its real title). Also set `PROPOSAL_LINK_SECRET` on Vercel production as part of this same remediation pass (was previously missing, blocking the password-protection feature from PR #77 in production).

**Addendum — second rotation, same day (2026-09-08):** the first rotation's new secret key value was printed in plaintext in a chat report (the redaction above), which is itself an exposure of the same kind this incident was about — logged so the record doesn't understate what happened. Rotated a second time immediately: new key generated and set on Vercel production, redeployed, confirmed live via the same three checks (homepage/proposal page/login all 200). Both prior keys — the original legacy service-role key and the first rotation's replacement — confirmed dead via direct REST calls to Supabase (`401`, in each case a server-issued rejection, not an assumption). **Standing rule adopted going forward: no secret/credential value is ever included in a report, chat message, or this log — confirmation of the action (generated / set / working / confirmed dead) only, never the literal string.** |
| Sep 8, 2026 | **Phase 0b — AI SDK upgrade path evaluated (again), and the Claude-primary/OpenAI-fallback harness wrapper built** (`docs/CORE_ENGINE_V2_SPEC.md` §3/§8) | 1) Upgrade the live intake pipeline to `ai@7.x`/`@ai-sdk/*@4.x` now. 2) Confirm the path and defer, build the harness on the currently-installed SDK instead. | **Deferred (upgrade), same conclusion as the Sep 4 entry — now with the exact breaking-change list. Harness wrapper built and proven live.** | Re-evaluated with the actual v7 migration guide against every real call site (not assumed): `maxTokens`→`maxOutputTokens` (all 9-10 sites), `tool({ parameters })`→`tool({ inputSchema })` (`app/api/chat/route.ts`), `result.toAIStreamResponse()` removed entirely (same file — replaced by a different wire protocol, not a rename), and the frontend breaks too — `NewProposalClient.tsx`'s `useChat` import from `'ai/react'` no longer exists in v4+ (moved to a new `@ai-sdk/react` dependency with a different hook API). Confirms the Sep 4 call: this is a full-pipeline migration, not a version bump, and stays out of scope for a single session. No `package.json` change made.<br><br>**Harness wrapper — built on the currently-installed SDK** (`ai@3.2.36`/`@ai-sdk/anthropic@0.0.36`/`@ai-sdk/openai@0.0.36`), deliberately not the upgraded one, so the existing pipeline stays genuinely untouched rather than nominally untouched: `lib/ai/stageConfig.ts` (single config source, one stage registered so far: `harness-check`), `lib/ai/harness.ts` (`runStage()` — Claude primary, one retry against OpenAI fallback on failure, logs which provider served the request via new `logAiProvider()` in `lib/logging.ts`), `app/api/dev/harness-check/route.ts` (the one new, low-stakes, authenticated-only proof call — not wired into any real proposal flow). `npx tsc --noEmit` clean; `git diff --stat` confirms only `lib/logging.ts` (+7 lines) plus the 3 new files touched, `package.json` untouched. Live-tested against local dev (colima + local Supabase started for this check, signed in as `test@invictus.app`): unauthenticated request → `401` as designed; authenticated request → `200`, `{ provider: 'openai', model: 'gpt-4o', usedFallback: true, object: { ok: true } }`. Server log confirms the exact mechanism: `[ai-harness] harness-check primary failed, retrying fallback Error [AI_LoadAPIKeyError]: Anthropic API key is missing` immediately followed by `[ai-harness] harness-check { provider: 'openai', model: 'gpt-4o', usedFallback: true }` — the retry-once-on-failure and provider-logging behavior both proven for real, not simulated. **Not yet proven: the primary (Claude) path succeeding** — `ANTHROPIC_API_KEY` is not set in local `.env.local` (only `OPENAI_API_KEY` is), so every local run so far has legitimately fallen through to OpenAI. Needs a real Anthropic key added locally to prove the primary path directly, rather than only its failure-and-recovery behavior. |
| Sep 8, 2026 | **Phase 1, sub-piece 1 — code generation + tagging contract, proven in isolation** (`docs/CORE_ENGINE_V2_SPEC.md` §2 stages 4-5, §4) | 1) Wire codegen into a real route now. 2) Isolated dev-only proof, same shape as Phase 0b's `harness-check`, not wired into any real flow. | **Isolated proof built and passed 3/3 live runs.** | Added a `'codegen'` stage to `lib/ai/stageConfig.ts` (same primary-Claude/fallback-OpenAI shape as `harness-check`) and a sibling `runStageText()` to `lib/ai/harness.ts` — codegen output is a long free-form HTML string, so `generateText` is the right primitive, not `generateObject` against an unwieldy single-string schema; same retry-once-to-fallback and provider-logging behavior as the existing `runStage()`. New `lib/ai/codegenPrompt.ts` builds the generation prompt from a deal-facts object + brand tokens (reuses `brandContextBlock()` from `lib/brand-extraction/prompt.ts` as-is), stating the model has full creative freedom over layout/copy/styling but must place three tagging attributes (`data-proposal-field="price_total"`, `data-proposal-field="due_date"`, `data-proposal-action="accept"`, exactly one of the last) on the real corresponding content, output as raw HTML only, no markdown fences. New `lib/ai/checkTags.ts` is the plain-code, no-model-call tag check (kept as its own file — stage 6's real deterministic-verification module will need this same shape later). No HTML parser existed in `package.json` — checked first, confirmed absent — added `node-html-parser` (9.0.4, two small deps) rather than hand-rolling fragile regex HTML matching. New `app/api/dev/codegen-check/route.ts` (authenticated via `getAccountContext()`, same pattern as `harness-check`) runs a hardcoded fixture — Bloom & Ives, $4,500 website redesign, due November 15 2026, 3 deliverables, 50/50 payment term — through the prompt and tag check.

`npx tsc --noEmit` clean. `git diff --stat` confirmed scoped: `lib/ai/stageConfig.ts` (+4), `lib/ai/harness.ts` (+36), `package.json`/`package-lock.json` (the one new dependency), plus the 3 new files — zero touch to the existing pipeline. **Live-tested 3 times against local dev (colima + local Supabase, signed in as `test@invictus.app`), all 3 passed:** every run returned `200`, `provider: 'openai'`, `model: 'gpt-4o'`, `usedFallback: true` (same expected reason as Phase 0b — no `ANTHROPIC_API_KEY` locally yet, primary fails cleanly and hands off every time, confirmed via the same `[ai-harness] codegen primary failed, retrying fallback` → `[ai-harness] codegen { provider: 'openai', ... }` log pair on all 3 runs), and `tagCheck.passed: true` on all 3 — `price_total`/`due_date` both present and containing the real fixture values, `accept` present exactly once (`count: 1`), not zero or duplicated. Manually inspected one full generated page: real, sensible HTML (a Tailwind Play-CDN-based styled document reflecting the fixture's brand colors/fonts/copy) with `<span data-proposal-field="due_date">November 15, 2026</span>`, `<span data-proposal-field="price_total">$4,500</span>`, and a single `<button data-proposal-action="accept">Accept Proposal</button>` — tags genuinely wrapping the real content, not just present somewhere in the page. Server-side Tailwind compile, deterministic verification as a real module, value injection, preview UI, and publish all remain explicitly deferred to later sub-pieces, per plan. |
| Sep 8, 2026 | **Phase 1, sub-piece 2 — deterministic verification + value injection, proven in isolation** (`docs/CORE_ENGINE_V2_SPEC.md` §2 stages 6-7, §4) | 1) Only check tagged values look right (sub-piece 1's level). 2) Overwrite tagged content with the real canonical value regardless of what the model wrote, so a wrong value becomes structurally impossible, not just detected. | **Built and proven — 3/3 live runs show a real pre-injection mismatch, corrected exactly post-injection.** | Generalized sub-piece 1's fixture-specific, boolean-only `lib/ai/checkTags.ts` into `lib/ai/verifyProposalTags.ts` — a real per-field report (`present`/`rawText`/`canonicalValue`/`matches`, not just pass/fail) plus `canonicalPrice()`/`canonicalDueDate()`. Price composes the two already-exported `lib/formatCurrency.ts` helpers (`currencySymbol()` + `formatAmount()`) rather than inventing new formatting logic. Due date is deliberately **pinned** to `toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })` rather than reusing the runtime-default-locale `.toLocaleDateString()` already used for `dateIssued`/`validUntil` elsewhere — a guardrail function can't inherit an ambiguity the rest of the codebase can tolerate in a cosmetic date. New `lib/ai/injectVerifiedValues.ts` mutates the same parsed tree `verifyProposalTags` just read (node-html-parser's `.set_content()`, confirmed by direct `node -e` test to mutate and correctly re-serialize via `.toString()`) and overwrites every *present* tagged element unconditionally — a missing tag is left alone and reported as a failure by `verifyProposalTags`, not invented by injection.

`lib/ai/checkTags.ts` deleted in the same change — confirmed via `grep -rn "checkTags"` that its only reference anywhere was the route this change also rewrites, so nothing else broke. **Fixture redesigned to actually exercise a correction:** sub-piece 1 handed the model an already-canonical string and checked it copied it; this sub-piece hands the model raw facts (`totalPrice: '4500 USD'`, `dueDate: 'the 15th of November, 2026'`) and lets it phrase them naturally, checked against a separate `SOURCE_OF_TRUTH` object (`{ priceTotal: 4500, currency: 'USD', dueDate: '2026-11-15' }`) via **exact** string match.

`npx tsc --noEmit` clean. `git diff --stat`: `lib/ai/verifyProposalTags.ts` + `lib/ai/injectVerifiedValues.ts` new, `lib/ai/checkTags.ts` deleted, `app/api/dev/codegen-check/route.ts` modified — nothing else. **3 live runs, all real mismatches, all corrected exactly:**
```
run 1 — price: rawText "$4500 USD" vs canonical "$4,500" → matches: false
        date:  rawText "the 15th of November, 2026" vs canonical "November 15, 2026" → matches: false
        accept: present, exactlyOne (count 1)
        post-injection tagged elements: "$4,500" / "November 15, 2026" — exact canonical values
run 2 — identical mismatch pattern, identical exact post-injection correction
run 3 — identical mismatch pattern, identical exact post-injection correction
```
All 3 runs demonstrate a real, non-contrived correction (not a lucky no-op) on both fields, every time — the model consistently wrote "$4500 USD" and a prose date, injection consistently overwrote both to the canonical strings byte-for-byte. Server log shows the same `AI_LoadAPIKeyError` → fallback mechanism as sub-piece 1/Phase 0b on all 3 runs (`provider: 'openai'`, `model: 'gpt-4o'`, `usedFallback: true`) — expected, no Anthropic credits yet.

**Explicit, not-silently-worked-around gap:** there is still no auto-repair or regeneration path for a *missing* tag. If `verifyProposalTags` reports `present: false` on any field, the route surfaces that as-is in the response — injection does nothing for that field, and nothing retries the generation. This session's fixture happened to produce all three tags present on all 3 runs, so the missing-tag path itself wasn't exercised live; building the retry/repair loop for it is an explicitly deferred future sub-piece, not something this change quietly papers over. |
| Sep 8, 2026 | **Phase 1, sub-piece 3 — server-side Tailwind compile, proven in isolation** (`docs/CORE_ENGINE_V2_SPEC.md` §2 stage 8, §7) | 1) Keep relying on the Play CDN/browser-JIT script the fixture output has used since sub-piece 1. 2) Compile real, finished CSS server-side, once, after generation — the Play CDN is dev-only per Tailwind's own docs and already rejected by the spec. | **Built and proven — 3/3 live runs, real compiled CSS confirmed by direct text inspection, not a render.** | **Part 1 research (done before any code):** the project runs Tailwind v4 (`tailwindcss@4.3.3`/`@tailwindcss/postcss@^4`), whose compile model differs materially from v3 — confirmed by reading the actually-installed package's own `.d.ts`, not assumed from v3 familiarity. `@tailwindcss/node@4.3.3` was already present in `node_modules` as an *undeclared* transitive dependency of `@tailwindcss/postcss`; it exports a real programmatic API, `compile(css, { base, onDependency }): Promise<{ build(candidates: string[]): string }>` — no project-wide file globs needed, just an array of class-name strings. Verified live with a standalone `node -e` call against the real installed package before writing any application code: candidates `['bg-[#3d4451]', 'text-2xl', 'font-bold', 'p-4', 'flex']` produced correct CSS for every plain utility *and* the arbitrary-value one. Declared `@tailwindcss/node` as an explicit direct dependency (`^4.3.3`) rather than leaving it an undeclared transitive one, since new application code now calls it directly.

**Part 2 build:** `lib/ai/compileTailwind.ts` (new) — `compileTailwindForHtml()` extracts candidates via a **broad regex text scan across the entire HTML string** (`html.match(/[^\s"'\`<>=]+/g)`, deduped), deliberately not attribute-specific `class="..."` parsing — a class added only via `element.classList.add(...)` inside a `<script>` block never appears in any class attribute, so attribute-only extraction would silently under-compile it. Over-matching from the broad scan (tag names, JS keywords, prose) is harmless since `.build()` only emits rules for tokens that are genuinely valid Tailwind utilities. `buildFinalArtifact()` strips the Play CDN `<script>` tag if present and inlines the compiled CSS into a `<style>` tag in `<head>`, returning **one self-contained HTML string** — a deliberate design choice, not a default: fits §8's versioned-storage model (one blob per generation) better than two correlated files that could drift apart. `lib/ai/codegenPrompt.ts` extended with two *guaranteed* (not left-to-chance) requirements: the model must use the brand's exact primary hex as an arbitrary-value class (e.g. `bg-[#2F6F4E]`), and must add the exact class `opacity-100` to an element via an inline `<script>`'s `classList.add('opacity-100')` while explicitly never placing that class in any static `class="..."` attribute — the specific case that proves the broad-scan extraction change actually matters, not just a theoretical concern.

**Real obstacle hit and fixed, outside the plan's originally-listed file scope — flagged, not silently absorbed:** the first live run failed with a hard `500` (Next.js's generic error page, not the route's own JSON error handler — the failure was at bundle time, before the route's try/catch could run). Root cause, from the server log: Turbopack couldn't statically bundle `lightningcss` (a transitive dependency of `@tailwindcss/node`), which resolves its native binary via a dynamic `require(...)` path — a well-documented category of Next.js bundling issue for native-addon packages. Fixed via Next.js's own documented mechanism: added `serverExternalPackages: ["@tailwindcss/node", "@tailwindcss/oxide", "lightningcss"]` to `next.config.ts`, telling Next.js to leave these packages un-bundled and let Node's own `require()` resolve them at runtime. This is a shared project config file the original plan didn't list — necessary, not optional (nothing could be verified without it), and called out explicitly here rather than treated as in-scope by default.

`npx tsc --noEmit` clean. `git diff --stat`: `lib/ai/compileTailwind.ts` new, `lib/ai/codegenPrompt.ts` + `app/api/dev/codegen-check/route.ts` + `next.config.ts` modified, `package.json`/`package-lock.json` for the one declared dependency. **3 live runs (after the config fix), all `200`, all three CSS checks passing by direct text inspection every time:**
```
run 1 — plain utility present: true (.rounded-lg, .flex, etc.)
        brand-hex arbitrary-value rule: .bg-\[\#2F6F4E\] { background-color: #2F6F4E; }
        script-only opacity rule: .opacity-100 { opacity: 100%; }
        opacity-100 absent from every static class="..." attribute: confirmed
        opacity-100 added only via classList.add('opacity-100') in <script>: confirmed
run 2 — identical pattern, same three checks pass
run 3 — identical pattern, same three checks pass
```
Verification (sub-piece 2's mechanism, still running in the same chain) also still shows genuine pre-injection mismatches corrected exactly on all 3 runs — no regression. Server log confirms the same `AI_LoadAPIKeyError` → OpenAI-fallback mechanism as every prior sub-piece, on all runs — expected, no Anthropic credits yet. No rendering, screenshot, or headless-browser step was used anywhere in this verification, per the plan's explicit instruction — every check above was a direct string/regex inspection of the returned CSS and HTML text.

**CORRECTION, dated Sep 9, 2026 (found incidentally during Phase 2 sub-piece 1's own verification, NOT part of that sub-piece's planned scope — a real bug in this already-shipped code, fixed as its own separate, explicitly-flagged change) — the "3/3 passing" claim above was true for the classes it actually checked, but incomplete as a general claim about CSS coverage.** The extraction regex (`/[^\s"'\`<>=]+/g`) excluded `'`/`"` entirely, which meant any arbitrary-value class containing an internal quote — e.g. `font-['Fraunces']` — got split at the quote and never compiled, silently. This was never exercised by this sub-piece's own checks (`bg-[#2F6F4E]`, `opacity-100`, neither containing an internal quote), so it went unnoticed until a longer, revision-generated page happened to use a quoted font-family arbitrary value for its FAQ heading. Confirmed the Tailwind compiler itself handles `font-['Fraunces']` correctly when given it as a whole candidate (tested directly, isolated from the extraction step) — the bug was purely the regex splitting the token at the internal quote before it ever reached the compiler.

**Fix:** widened `lib/ai/compileTailwind.ts`'s pattern to a two-alternative form — a bracketed-group alternative tried first that treats an entire `[...]` as opaque (anything but `]` allowed inside, quotes included), falling back to the original plain-token pattern for everything without brackets. Verified directly against `font-['Fraunces']` specifically, both at the extraction-regex level and end-to-end through the real compiler, before trusting it in the full chain. Also added a fifth guaranteed requirement to `buildCodegenPrompt` (`lib/ai/codegenPrompt.ts`) — a quoted arbitrary-value font class, `font-['<heading font>']`, required on every generation going forward, so this exact gap can never silently regress unnoticed again. Re-verified across Phase 2 sub-piece 1's final 3 verification runs: all 3 showed both `font-['Fraunces']` and `font-['Inter']` quoted arbitrary-value classes with real compiled CSS rules present for each, confirmed via direct inspection of the stored HTML — not just re-running the same single case once. This line above stands as the accurate historical record of what was actually verified at the original time, not silently edited away. |
| Sep 8, 2026 | **Phase 1, sub-piece 4 — sandboxed preview, proven in isolation, confirmed by a human in a real browser** (`docs/CORE_ENGINE_V2_SPEC.md` §2 stage 9, §6) | 1) Trust the "srcdoc + sandbox=\"allow-scripts allow-same-origin\"" combination without checking it. 2) Verify the claim against real, current sources before building anything, since getting this wrong would silently defeat the entire security boundary. | **Claim confirmed and corrected with precision via real MDN citations — final config is `sandbox=\"allow-scripts\"` alone. Isolation confirmed genuinely working via a human opening the page in a real browser, not a self-certified or automated check.** | **Part 1 research, with citations:** MDN's `HTMLIFrameElement: srcdoc` property page — *"The `srcdoc` property allows absolutely any HTML markup to run in a frame by default. If the frame is not sandboxed using the CSP `sandbox` directive (or is sandboxed but includes the `allow-same-origin` value) then it will be same-origin with the parent,"* with its own explicit mitigation: *"If the frame is not expected to need access to your parent document, you can mitigate the risk by using a CSP sandbox without the `allow-same-origin` value. The frame will then be treated as a cross-origin resource."* (https://developer.mozilla.org/en-US/docs/Web/API/HTMLIFrameElement/srcdoc). MDN's `<iframe>` element reference confirms the underlying mechanism — without `allow-same-origin`, *"the resource is treated as being from a special origin that always fails the same-origin policy"* — and separately warns that `allow-scripts` + `allow-same-origin` together on a same-origin document is *"strongly discouraged... as that lets the embedded document remove the `sandbox` attribute — making it no more secure than not using the sandbox attribute at all"* (https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/iframe). This confirmed the exact concern raised before any code was written: for `srcDoc` specifically (no URL of its own to derive a separate origin from), adding `allow-same-origin` would have made the framed content same-origin with the parent — defeating isolation entirely. **Final, deliberate config: `sandbox="allow-scripts"` alone** — scripts run (needed for the guaranteed `opacity-100` fade-in from sub-piece 3), the frame gets an opaque/null origin that always fails the same-origin check against the parent.

**Build:** `app/dev/preview-check/page.tsx` (new) — a real browsable Server Component page, not a JSON API route, since the actual proof requires a human opening it in a real browser; same auth pattern as the rest of the app (`getAccountContext()` + `redirect('/login')`). Reuses the exact same generate→verify→inject→compile chain as `app/api/dev/codegen-check/route.ts`, just called from a page instead of returning JSON, and renders `<iframe sandbox="allow-scripts" srcDoc={finalHtml} />`. `lib/ai/codegenPrompt.ts` extended with a sixth guaranteed requirement (same "don't leave it to chance" pattern as sub-piece 3): the generated page's own inline `<script>` must attempt `window.parent.document` in a `try/catch` and visibly write "BLOCKED — ..." or "NOT BLOCKED — isolation failed" into a `<div id="isolation-check">` in the rendered page itself — making the isolation check something a human can see, not just something asserted in a report. No Playwright, no headless check, no automated self-certification anywhere in this piece, per the plan.

**Human verification result, real browser, confirmed by Sahil (not self-certified):** the on-page `isolation-check` div read exactly *"BLOCKED — SecurityError: Failed to read a named property 'document' from 'Window': Blocked a frame with origin \"null\" from accessing a cross-origin frame."* — genuine browser-generated security-error text, origin `"null"` exactly as the Part 1 research predicted for a sandbox without `allow-same-origin`. Isolation confirmed working as designed, by direct human observation of a real error the browser itself produced, not a mocked or scripted result. **Additional real-world confirmation, incidental but valuable:** Sahil also observed the rendered page visibly showing the canonically-injected values ($4,500 / November 15, 2026) — sub-piece 2's injection mechanism confirmed working correctly in an actual rendered page, not just in the raw API response text inspected in earlier sub-pieces.

`npx tsc --noEmit` clean. `git diff --stat`: `app/dev/preview-check/page.tsx` new, `lib/ai/codegenPrompt.ts` modified — nothing else. |
| Sep 9, 2026 | **Phase 1, sub-piece 5 (final piece) — versioned storage + isolated-origin publish** (`docs/CORE_ENGINE_V2_SPEC.md` §2 stage 11, §6, §8, §9) | 1) Defer the `generated_pages` schema again (§9 had explicitly flagged it as undesigned). 2) Design it for real this session, plus prove serving from a genuinely separate origin. | **Built and proven — real row published, isolation confirmed by a human via a real screenshot, not self-certified.** | **Part 1 research, with a real correction to a starting assumption:** confirmed via Next.js's own current docs that `middleware.ts` is deprecated and renamed to `proxy.ts` as of v16.0.0 (this project runs Next.js 16) — and, checking the repo *before* planning further, found `proxy.ts` already exists at the root, already migrated to this convention (dated Sep 1), delegating entirely to `updateSession()` (`lib/supabase/middleware.ts`) for `@supabase/ssr` auth-cookie refresh. This changed the plan materially: no second file (impossible — one export per file) — the host-based preview-routing logic went *inside* the existing `proxy()` function, checked *before* `updateSession()` runs. Confirmed `*.localtest.me` is a real, legitimate public DNS record (not a proxy service — *"the entire domain name localtest.me — and all wildcard entries — point to 127.0.0.1"*) that produces genuinely distinct browser origins per subdomain, and — unlike bare `localhost`, not a registrable domain per RFC 6761 — is a real registrable domain, so ordinary host-only cookies behave exactly as in production.

**Schema, columns flagged plainly as genuinely new design, not a mechanical extension:** `generated_pages` — `id`, `proposal_id` (FK to `proposals`, `ON DELETE CASCADE`), `version` (int, app-computed max+1), `html`, `provider`/`model`/`used_fallback` (carried from the harness result), `created_at`, `published_at` (nullable — a row can exist generated-but-unpublished). RLS enabled with **no policies** plus an explicit `REVOKE ALL ... FROM anon, authenticated` — service-role-only access, stated outright rather than assumed, same rigor as the earlier anon-grant tightening. One dedicated test proposal row inserted directly (not via migration — fixture data, not schema), `status = 'DRAFT'`, clearly named `SUBPIECE5-TEST-DO-NOT-USE`, under the existing local dev test account — none of the 5 real proposals touched.

**Two gaps flagged plainly, not glossed over, per explicit instruction before building:**
1. **Version race condition:** `publishGeneratedPage()` computes `max(version)+1` in application code, then inserts — a real race under concurrent publishes for the same `proposal_id`. Harmless today (only this one test proposal, nothing else calls this function), but a unique constraint + retry (or a serializable transaction) is the real fix before this could handle genuine concurrent publishes. Tracked here the same way missing-tag auto-repair already is.
2. **`proxy.ts` is a shared, modified file — not equivalently isolated to the previous four sub-pieces' purely-additive new files.** The new preview-host-serving branch and the marker-cookie branch both live inside the one file every request in the app already passes through. The marker-cookie logic is gated behind `ENABLE_PREVIEW_ISOLATION_TEST` (unset/false by default, including real production — with it unset, `proxy.ts`'s behavior is byte-for-byte identical to before this change), but the file itself is genuinely shared now, and that's a materially different kind of change from every prior sub-piece and is recorded as such, not treated as equally contained.

`npx tsc --noEmit` clean. `git diff --stat`: new migration (`20260908184816_add_generated_pages.sql`), `lib/ai/publishGeneratedPage.ts` new, `proxy.ts` **modified** (pre-existing file, per the Part 1 finding above), `lib/ai/codegenPrompt.ts` + `app/dev/preview-check/page.tsx` modified. **Chain run confirmed with real, independent evidence, not just trusting the page's own output:** the page reported `published: generated_pages id=f3e0b50a-4ca4-4778-a611-28d254f13817, version=1`; a direct `psql` query against that exact row independently confirmed `proposal_id` = the dedicated test proposal, `version=1`, `provider=openai`, `model=gpt-4o`, `used_fallback=t`, and — critically — `published_at` actually set (`2026-09-08 18:51:40.033+00`), not null. Marker-cookie presence on the main app confirmed directly (`document.cookie.includes('subpiece5_isolation_marker')` → `true`) before handing off for human verification. A `curl` sanity check against the preview host (spoofed `Host` header, not real DNS) confirmed `200`, correct `content-type: text/html; charset=utf-8`, the real generated title, zero dashboard/nav chrome, and both isolation-check divs present in the markup — deliberately stopping short of reading what they said, leaving the actual security confirmation to a human.

**Human verification result, real browser, confirmed by Sahil with a real screenshot (not self-certified):** the `cookie-isolation-check` div on `http://preview.localtest.me:3000` read *"BLOCKED — cookie not visible from this origin"* — genuine origin-level isolation via subdomain (not iframe sandboxing this time) confirmed working. The `isolation-check` div (sub-piece 4's iframe-specific check, still present in the prompt) correctly read *"NOT BLOCKED"* — expected and harmless, since there's no iframe parent on a top-level page for `window.parent` to differ from `window` itself; this is not a regression or a real isolation failure, just a check that's inapplicable outside an iframe context. Also confirmed: signing in and using the real dashboard still worked normally after the `proxy.ts` change — the shared-file modification (gap #2 above) did not break existing auth/session behavior. |
| Sep 9, 2026 | **Phase 2, sub-piece 1 — single bounded revision round, proven in isolation** (`docs/CORE_ENGINE_V2_SPEC.md` §2 stage 10, §8, §9) | §9's open question: full prior HTML as revision context, or a compact summary? | **Full HTML — confirmed the right call with real token figures, not asserted.** | Full markup (post-injection, pre-compile — not the stored post-compile blob, which has ~7-10KB of compiled CSS the model doesn't need to see) fed as revision context across all 3 final runs cost `promptTokens` of 1192, 1195, 1183 — small, predictable, nowhere near a real context-length concern for a bounded 2-3 round loop. `completionTokens` 913, 920, 906. No summary needed; the recommendation holds on real evidence.

**Built:** `lib/ai/revisePrompt.ts` (new) — full-page regeneration from feedback, restating only `TAGGING_CONTRACT_BLOCK` (newly extracted from `lib/ai/codegenPrompt.ts`'s previously-inline tagging-contract text, exported so this file composes with it instead of duplicating it — sub-piece 3-5's one-off proof requirements, items 1-4 in `buildCodegenPrompt`, are deliberately NOT restated for a revision, since the prior HTML already has them). New `'revise'` entry in `lib/ai/stageConfig.ts`, same primary/fallback shape as `'codegen'`. `runStageText()` in `lib/ai/harness.ts` now returns a real `usage: { promptTokens, completionTokens }` field read directly from `generateText`'s own result — the only way to report real, not estimated, token figures. `app/dev/preview-check/page.tsx` extended to run v1 (generation) then v2 (revision, feedback: "Add a short FAQ section before the Accept button.") through the identical verify→inject→compile→publish chain for both — no new guardrail logic, proving the existing chain holds on round 2, not just round 1.

**Round cap:** enforced with `MAX_VERSIONS = 3`, tested directly and cheaply (not via the real AI pipeline) — 4 sequential `publishGeneratedPage()` calls with placeholder content against one dedicated throwaway proposal: calls 1-3 succeeded (versions 1, 2, 3), call 4 threw `Proposal ... has reached the 3-version cap` exactly as designed.

**A real, unplanned bug found and fixed mid-verification — full account, not glossed over:** the first 3 verification attempts (against `publishGeneratedPage`'s original select-then-insert implementation, unchanged since sub-piece 5) published v2 as `version=1` instead of `version=2` — confirmed via direct `psql` query showing two rows both claiming `version=1` for the same `proposal_id`, 7 seconds apart (sequential, not concurrent). Initial hypothesis: Next.js's fetch request memoization (confirmed as a real, current Next.js 16 behavior — *"Identical fetch requests in a React component tree are memoized by default"*, https://nextjs.org/docs/app/getting-started/fetching-data) returning a stale "0 existing rows" result for the second of two structurally-identical version-lookup calls within one Server Component render. **This hypothesis was tested directly and NOT confirmed:** adding a custom `fetch` with `cache: 'no-store'` to just the version-lookup call, changing nothing else, and re-running against a fresh proposal — still produced two `version=1` rows. Per Next.js's own docs, `cache: 'no-store'` is a Data Cache directive and is a separate layer from Request Memoization, so this result doesn't rule memoization out either — it specifically shows that lever doesn't fix it. **Recorded honestly as an open, now-moot question: the exact prior mechanism was never confirmed, only a plausible-but-untested-to-completion hypothesis.** It's moot because the actual fix doesn't depend on identifying it: replaced the two-step select-then-insert with one atomic Postgres function, `public.publish_generated_page()` (new migration `20260909030806_add_publish_generated_page_function.sql` — computes `next_version` and inserts in a single round trip, serialized per-`proposal_id` via `pg_advisory_xact_lock`), called via `.rpc()` instead of separate `.select()`/`.insert()` calls. This structurally can't be affected by request memoization (one fetch call, not two identical ones) regardless of whether that was ever the real cause, and it also resolves the previously-flagged concurrent-write race condition as a direct side effect — both closed by the same change. Round cap re-tested clean after this fix (same 1→2→3→throw result). Per instruction, no broader codebase grep for similar sequential-read-then-write patterns was done, since the memoization explanation was never actually confirmed — that grep was conditioned on confirmation.

A second, separate bug was found while spot-checking sub-piece 1's own verification requirement ("compiled CSS covers v2's actual classes") — **not part of this sub-piece's own scope, logged and fixed as its own distinct change; see the correction note on the Phase 1 sub-piece 3 entry above for the full account.**

**Final verification, after the version-numbering fix, 3 fresh test proposals (never reused):** all 3 runs' `generated_pages` rows independently confirmed via direct `psql` query at exactly versions 1 and 2 (no duplicates, no gaps) for each of the 3 proposal ids. All 3 runs' v2 content confirmed to contain a real "Frequently Asked Questions" section (not just re-generated noise). `promptTokens`/`completionTokens` per run: (1192, 913), (1195, 920), (1183, 906) — consistent, small, real figures, confirming §9's full-HTML-context decision at the top of this entry. `npx tsc --noEmit` clean throughout. `git diff --stat` for this sub-piece's own commit: `lib/ai/revisePrompt.ts` new; `supabase/migrations/20260909030806_add_publish_generated_page_function.sql` new; `lib/ai/stageConfig.ts`, `lib/ai/harness.ts`, `lib/ai/publishGeneratedPage.ts`, `app/dev/preview-check/page.tsx` modified; `lib/ai/codegenPrompt.ts` modified only for the tagging-contract extraction (`TAGGING_CONTRACT_BLOCK`) this sub-piece needed to reuse — the separate quoted-arbitrary-value fix to that same file ships in its own commit, described in the correction note above. Missing-tag auto-repair remains deferred from Phase 1 sub-piece 2. |
| Sep 9, 2026 | **Phase 2, sub-piece 2 — chained multi-round revision (2-3 rounds), proven in isolation** (`docs/CORE_ENGINE_V2_SPEC.md` §2 stage 10, §8) | Sub-piece 1's `MAX_VERSIONS = 3` (v1 + 2 revisions) vs. §8's "2-3 rounds" language, whose upper bound means v1 + 3 revisions = 4 total. | **Raised to 4** — sub-piece 1's real, observed per-round cost (~1183-1195 prompt / ~906-920 completion tokens) showed no cost reason to hold the cap below the spec's own stated upper bound, so the decision followed the data rather than picking a number first. | Composition of what already existed, not new mechanism: `app/dev/preview-check/page.tsx` restructured around a local `runRound()` helper (parse → verify → inject → compile → combine — the exact same chain sub-piece 1 hand-inlined twice, now factored out since inlining it 4 times would be real duplication) and a loop over 3 distinct, concrete, unrelated feedback strings — round 1: "Add a short FAQ section before the Accept button.", round 2: "Make the tone more premium and upscale throughout.", round 3: "Add a one-sentence testimonial quote near the top, attributed to 'a past client'." `priorHtml` is reassigned to the current round's markup at the end of each loop iteration — never accumulated — so each revision prompt sees only the immediately-prior version's full HTML, confirmed both by the code and by the real token data below (no doubling/compounding pattern). `lib/ai/publishGeneratedPage.ts`'s `MAX_VERSIONS` bumped 3→4 (a parameter passed into the already-built `publish_generated_page()` function each call, so no migration change needed).

**Round cap re-tested at the new value, cheap and direct (no model calls):** one dedicated throwaway proposal, 5 sequential `publishGeneratedPage()` calls with placeholder content — calls 1-4 succeeded (versions 1-4), call 5 threw `Proposal ... has reached the 4-version cap` exactly as designed.

`npx tsc --noEmit` clean. `git diff --stat`: `lib/ai/publishGeneratedPage.ts` (the `MAX_VERSIONS` bump) and `app/dev/preview-check/page.tsx` (the loop restructure) modified — nothing else.

**3 full v1→v2→v3→v4 verification runs, each on its own fresh test proposal, all identical and clean:**
```
run 1 — v2 (FAQ):        promptTokens=1215, completionTokens=936
        v3 (tone):        promptTokens=1429, completionTokens=936
        v4 (testimonial): promptTokens=1439, completionTokens=975
run 2 — v2: 1197/918   v3: 1411/918   v4: 1421/957
run 3 — v2: 1215/936   v3: 1429/940   v4: 1443/979
```
Per-round cost stays in a narrow band (~1200-1440 prompt tokens) across all 3 runs — the modest round-over-round increase (roughly +200 tokens from v2→v3, then flat or near-flat v3→v4) is consistent with the page itself growing slightly as content is added each round, not context accumulation: a real accumulation bug (each round's prompt carrying every prior round's HTML) would show roughly doubling per round (v2~1200 → v3~2400+ → v4~3600+), which none of the 3 runs show. `verifyProposalTags`/injection/compile all passed cleanly (`allPresent: true`) at every one of the 4 versions, in every run — 12 clean checks total, not just the final version each time.

**The actual point of this sub-piece — content persistence across unrelated rounds, confirmed via direct `psql` inspection of every stored version's HTML, not assumed from the code structure:**
```
              v1        v2 (+FAQ)   v3 (+tone)   v4 (+testimonial)
FAQ present:   no         yes          yes            yes
testimonial:   no         no           no             yes
```
Identical pattern across all 3 runs — v4 genuinely retains the FAQ section from round 1's feedback despite two further, unrelated rounds in between; full-page regeneration is treating the immediately-prior version as authoritative rather than silently dropping earlier changes. Also spot-checked the harder-to-grep "premium tone" round qualitatively: v2's opening line reads *"We are excited to present our proposal..."*, v3's (same run, after the tone-shift feedback) reads *"We are delighted to present our proposal... Our vision..."* — a real, visible wording shift, not a no-op that just carried v2's copy forward unchanged. |
| Sep 9, 2026 | **Phase 1 gap closure — missing-tag auto-repair** (`docs/CORE_ENGINE_V2_SPEC.md` §4) | The gap flagged since Phase 1 sub-piece 2: `verifyProposalTags` reports a missing tag as a failure, nothing attempts a fix. | **Closed — bounded retry built, reusing the existing revise pathway, proven across all 4 required scenarios with real evidence.** | **Two gaps in the given design, resolved before building:** (1) the stated `attemptAutoRepair` signature had no way to re-verify a repair — `verifyProposalTags` needs a `ProposalSourceOfTruth` (canonical values), a different, smaller object than `CodegenDealFacts` (display strings) — added `sourceOfTruth` as a required parameter. (2) Stated explicitly, not silently followed: `verifyProposalTags`'s `allPresent` checks `acceptAction.present` but not `acceptAction.exactlyOne` — a page with *two* accept buttons would already read `allPresent: true` and never enter repair. Consistent with "missing-tag" being the stated scope, but a duplicated accept action is a real, different failure mode this mechanism does not attempt to fix; not built here.

**Extracted first, to avoid duplicating what two dev routes now both need:** `lib/ai/fixtures.ts` (the shared `FIXTURE_FACTS`/`SOURCE_OF_TRUTH`/`FIXTURE_BRAND_KIT`, previously local consts in `preview-check/page.tsx`) and `lib/ai/stripCodeFence.ts` (same move). `preview-check/page.tsx` updated to import both — behavior unchanged.

**`lib/ai/autoRepair.ts`** (new) — `attemptAutoRepair()`: returns immediately (`attemptsUsed: 0, repaired: false`, zero model calls) if `allPresent` is already true. Otherwise loops up to `maxAttempts` (default 2): builds a feedback string naming the specific missing element(s) by their real attribute (e.g. `data-proposal-action="accept" (on the single accept/sign button)`, not a vague "fix this"), passes it through `buildRevisePrompt` (Phase 2, reused not reinvented) and `runStageText('revise', ...)`, re-verifies, stops as soon as `allPresent`. Does not inject, compile, or publish itself — returns pre-injection markup, hands back to the existing chain, which the caller runs only on the final outcome.

**Real test scaffolding needed, not organic model failure:** the model is normally always instructed to include all three tags, so a genuine missing-tag case had to be *forced*, not hoped for. Built a TEST-ONLY `BROKEN_TAGGING_CONTRACT_TEST_ONLY` (omits the accept-action instruction entirely) in a new dedicated proof route, `app/dev/auto-repair-check/page.tsx` — kept out of `lib/ai/` deliberately, so it can't be mistaken for production prompt logic.

**A real, corrected mistake in the exhaustion-test design, not glossed over:** the first attempt at forcing Scenario C (exhaustion) reused the same broken-tagging-contract trick as the injectable `buildPrompt` for repair attempts too — and it did NOT reliably force failure. `attemptAutoRepair`'s own `describeMissingTags()` feedback string names the exact missing attribute by name, and that specific, targeted instruction gave the model enough signal to add it correctly anyway, regardless of what the surrounding broken template omitted (confirmed live: first run showed `attemptsUsed: 1, repaired: true` when exhaustion was expected). **Fix:** the exhaustion-only prompt builder also discards the informative feedback itself, replacing it with a generic, useless instruction ("Make the page look nicer overall.") — so the model genuinely has no signal about what's missing on any attempt, not just a template that's silent about it while the feedback string still gives the answer away.

`npx tsc --noEmit` clean. `git diff --stat`: `lib/ai/autoRepair.ts`, `lib/ai/fixtures.ts`, `lib/ai/stripCodeFence.ts`, `app/dev/auto-repair-check/page.tsx` new; `app/dev/preview-check/page.tsx` modified (import de-duplication only).

**All 4 required scenarios, real evidence:**
- **Scenario A (repair succeeds), 3 runs, each a fresh proposal:** every run's broken-prompt generation genuinely produced `acceptAction: {present: false, count: 0}` — confirmed, not assumed. Every run repaired on the first attempt (`attemptsUsed: 1, repaired: true`), final verification `allPresent: true` in all 3. Published as version 1 in all 3 — confirmed via direct `psql` query that each proposal has **exactly one** `generated_pages` row, not one per repair attempt: the repair mechanism does not consume a version slot, exactly as designed.
- **Scenario B (no-op):** normal, working prompt → `allPresent: true` → `attemptAutoRepair` returned `attemptsUsed: 0, repaired: false`. Confirmed via real call-count evidence, not the return value alone: counted `[ai-harness] revise` success lines in the server log immediately before (3, from the Scenario A runs) and immediately after (still 3) — zero new revise calls from the no-op path.
- **Scenario C (exhaustion), after the design fix above:** `attemptsUsed: 2, repaired: false`, final verification still `acceptAction.present: false` — the cap fired at exactly `maxAttempts`, not looping past it or succeeding by luck. `publishGeneratedPage` is never called anywhere in this code branch, unconditionally — confirmed by the code path itself, not inferred from a report.

Missing-tag auto-repair is closed. Still open: the `acceptAction.exactlyOne` failure mode (flagged above, deliberately out of this task's scope) and Phase 3 (visual self-check loop). |
| Sep 9, 2026 | **New engine — first real integration point** (`docs/CORE_ENGINE_V2_SPEC.md` §9) | 1) Wait for Phase 3 (visual self-check) before touching any real path. 2) Wire the already-proven Phase 1+2 chain into one narrow, reversible, own-account-only real entry point now. | **Wired in — feature-flagged, own account only, existing schema-based path untouched and still primary.** | Everything before this ran only through dev-only routes and hardcoded fixtures (`FIXTURE_FACTS`/`FIXTURE_BRAND_KIT`/`SOURCE_OF_TRUTH`). This is the first time any of it touches a real account, a real saved proposal, and a real (if still test/beta) published URL.

**Flag — `lib/betaFlags.ts`, env var only, no migration:** `isBetaAiEngineEnabled(accountId)` checks a comma-separated `BETA_AI_ENGINE_ACCOUNT_IDS` allowlist. Unset by default (off for everyone). Set locally to Sahil's real account id only. Checked **server-side in the API route itself**, not just used to hide a button — a direct request from a non-flagged account is genuinely refused, not merely kept out of the UI.

**`lib/ai/mapProposalToDealFacts.ts`(new) — the one genuinely new piece of work, two real schema mismatches resolved and stated, not silently picked:**
1. Real proposals have multiple tiered packages; the new engine's `CodegenDealFacts`/`ProposalSourceOfTruth` support exactly one price. Resolved by using the `popular`-flagged package, or the first if none is marked. **Stated limitation:** a multi-tier proposal's other packages never appear anywhere in the new-engine version — fine for a single-account experiment, a real gap before this goes further.
2. Real proposals have relative phase durations (`timeline: [{duration: "2 weeks"}]`), not a fixed due date. Resolved by parsing and summing each phase's duration from `dateIssued`. **Deliberately conservative, confirmed against real local data before committing to the pattern:** only the exact whole-string shape `^\d+\s+(day|days|week|weeks|month|months)$` (case-insensitive) counts as parseable — a range like `"2-3 weeks"` or anything with extra words is treated the same as unparseable, never reduced to "the first number found." Real local timeline data was queried before building this, not assumed clean: alongside clean cases (`"1 week"`, `"2 weeks"`, `"5 weeks"`) it also contains `""` and `"Monthly"` — both correctly rejected by this pattern. If *any* phase fails to parse, the whole calculation falls back to `validUntil` + 30 days, never a partially-trusted mixed result.

**A real, non-fixture bug found and fixed via the real verification run itself:** the first `toIsoDate()` used `date.toISOString().slice(0, 10)`. Tested against Sahil's real "Simple Dental Clinic" proposal (`dateIssued` "9/6/2026", timeline phases "1 week"/"2 weeks"/"3 weeks", all cleanly parseable = 42 days, correctly landing on local October 18, 2026) — the `due_date` tag rendered "October 17, 2026", one day off. Root cause: on a positive-UTC-offset server (IST, UTC+5:30), local midnight Oct 18 is Oct 17 18:30 UTC, so `.toISOString()` silently shifts the calendar date back a day before `.slice(0, 10)` ever runs — a bug that cannot surface against fixture data built with a UTC-safe date string, only against a real proposal's real local-timezone date arithmetic. Fixed by extracting `getFullYear()`/`getMonth()`/`getDate()` directly, never round-tripping through UTC. Re-ran the same real proposal after the fix (version 2): `due_date` corrected to "October 18, 2026", `price_total` unaffected ("$70,000").

**`app/api/proposals/[id]/beta-ai-page/route.ts`** (new) — 401 if unauthenticated, 403 if `isBetaAiEngineEnabled` is false for the caller's account (checked before any proposal lookup), 404 if the proposal isn't found under the caller's own RLS scope, then the **exact, unmodified Phase 1+2 chain**: `buildCodegenPrompt` → `runStageText('codegen')` → `verifyProposalTags` → `attemptAutoRepair` (only if needed) → `injectVerifiedValues` → `compileTailwindForHtml` → `buildFinalArtifact` → `publishGeneratedPage`. No new pipeline logic anywhere in this route beyond the mapping above.

**UI — `NewProposalClient.tsx`'s existing "Confirm before we build" modal:** one new button, "Also try the new AI page designer (beta)", rendered only when `betaAiEngineEnabled` (a server-resolved prop from `page.tsx`, not a client-side-only check) is true. The existing plain "Generate proposal" button is unchanged and stays the default, primary path.

**`proxy.ts`** — `servePreviewHost()` now takes an optional `slug`; given one, it joins `generated_pages` to `proposals.slug` and serves that specific proposal's most-recently-published page. No slug (every existing dev-proof route's usage) falls back to the original unfiltered "most recent published row" query, unchanged — nothing built in Phase 1+2 breaks.

**Verification:** `npx tsc --noEmit` clean. `git diff --stat`: `app/dashboard/proposals/new/NewProposalClient.tsx`, `app/dashboard/proposals/new/page.tsx`, `proxy.ts` modified; `lib/betaFlags.ts`, `lib/ai/mapProposalToDealFacts.ts`, `app/api/proposals/[id]/beta-ai-page/route.ts` new — nothing else. Real isolated-origin publish confirmed for the real "Simple Dental Clinic" proposal at `http://preview.localtest.me:3000/transforming-simple-dental-clinic-a-strategic-design-and-bui-0d85b277`: `200`, correct `content-type`, correct real title, zero dashboard chrome (`grep -c "Your proposals\|Skip to content"` → `0`), real compiled CSS present (2 `<style>` blocks in the artifact, not a bare unstyled fragment). Root path (no slug) re-confirmed still serving the original no-filter behavior, unbroken.

**Flag-gating confirmed by direct function evidence, not by mutating a real (test) account's credentials to force a live session:** calling the actual `isBetaAiEngineEnabled()` against the real local `BETA_AI_ENGINE_ACCOUNT_IDS` value returned `true` for Sahil's account and `false` for the standing non-flagged test account (`test@invictus.app`, `b7c1dbac-f522-416d-be2e-c1e2ba0918d9`) and for `null`/`undefined`. Since the API route calls this exact function, unconditionally, as its first branch — before any proposal lookup — and the UI button's visibility is driven by the same function's result computed server-side in `page.tsx`, this is a direct proof of both gates, not an inference. Stopped short of a full live-session HTTP 403 request test: doing so would have required resetting the test account's password to force a session, which the environment's own permission classifier correctly declined as a credential-changing action outside this task's scope — flagged here rather than worked around.

**Explicitly not yet done, per the plan's own verification section:** Sahil has not yet created a real proposal end-to-end through the actual product UI using the new beta button himself — every real-data check above used one of his pre-existing real proposals as a non-destructive technical sanity check, deliberately not simulating his own first real use of it. Nothing here has been committed pending that step and his own review. |
| Sep 9, 2026 | **Real-account regression from Sahil's first actual end-to-end beta run** (`docs/CORE_ENGINE_V2_SPEC.md` §9) | 1) Leave `buildCodegenPrompt` as-is, since it worked in every dev-proof run. 2) Split it — a real, customer-facing generation must not carry dev-only proof scaffolding regardless of whether the dev routes happened to need it too. | **Split. Fixed and re-verified on the same real proposal.** | His real run surfaced two visible "Checking..." lines at the bottom of the generated page — the `#isolation-check`/`#cookie-isolation-check` divs from Phase 1 sub-pieces 4-5's dev-proof requirements. Confirmed root cause: `buildCodegenPrompt` — the same function `beta-ai-page/route.ts` calls for real generation — unconditionally included all of sub-piece 4-5's proof-only additional requirements (isolation-check div, cookie-isolation-check div, a script-only `opacity-100` class, and a synthetic arbitrary-hex-class requirement), never split out from the three dev-proof routes that legitimately still need them to keep exercising that mechanism.

**`lib/ai/codegenPrompt.ts` restructured around a shared `buildCodegenPromptCore()`:** `buildCodegenPrompt` (real, used only by `beta-ai-page/route.ts`) now emits `TAGGING_CONTRACT_BLOCK` plus exactly one additional requirement — the quoted brand-heading-font arbitrary-value class — kept because it drives a real, visible piece of the page (the actual brand font), unlike the other four items which have zero customer-facing value. `buildCodegenPromptTestOnly` (new) adds the four dev-proof-only items back via `devProofRequirementsTestOnly()`. `app/api/dev/codegen-check/route.ts`, `app/dev/preview-check/page.tsx`, and `app/dev/auto-repair-check/page.tsx` — the three routes that actually need those proof requirements — repointed to the test-only variant; nothing else changed in any of them.

`npx tsc --noEmit` clean. **Re-verified on the same real "Simple Dental Clinic" proposal, not a fixture:** regenerated via `beta-ai-page` (now v3) and confirmed, by direct `grep` on the served artifact, zero occurrences of `"Checking..."`, `isolation-check`, `subpiece5_isolation_marker`, or `opacity-100` anywhere in the output. Confirmed the fix didn't silently break anything else in the same pass: `price_total` still `$70,000`, `due_date` still "October 18, 2026" (the fix from the prior entry held), `accept` action still present exactly once, compiled CSS still present (2 `<style>` blocks).

**Second finding, investigated and reported, deliberately not fixed here:** why the real page looked generically styled rather than on-brand. Root cause confirmed directly against the DB, not assumed: `brand_kits` has **zero rows** for Sahil's account, and all three of his real proposals have `brand_kit_id` NULL/empty. `resolveBrandKit()`'s own first guard (`typeof brandKitId !== 'string'`) returns `null` before ever issuing a query in this case — this is the function behaving correctly, not a bug. The real run therefore rendered with the plain code-default fallback (`'Georgia'` heading font, no brand context block contributed to the prompt at all) because no brand kit exists yet to resolve, consistent with his own dashboard still showing "Set up your brand kit" as an unfinished setup step. **Logged as an open product decision, not solved here:** whether the no-brand-kit default needs real design work (a considered fallback palette/typography, not just "whatever Georgia produces") before this goes beyond a one-account beta, or whether requiring a brand kit before offering the beta button is the better fix. |
| Sep 9, 2026 | **Free-tier "1 active proposal" limit — enforced, closing half of the Sep 8 credits/usage gap** | 1) Wait for the Stripe-replacement decision, since that entry deferred all metering together. 2) Enforce only the flat proposal-count cap now — it needs no billing-provider decision, unlike pay-per-proposal metering or `credit_transactions`. | **Enforced. The pay-per-proposal/credits half of the Sep 8 entry stays open, unchanged.** | The Sep 8 entry logged that the free tier's own pricing copy ("1 active proposal", `SettingsClient.tsx`) had zero enforcement anywhere. Confirmed, not assumed, before building: every real proposal-creation path (`POST /api/proposals` — the one route the real intake UI and any direct API call both hit; `duplicateProposalAsDraft()` in `app/dashboard/actions.ts`, a second independent insert path); the "active" definition (`status != 'ARCHIVED'` — the only put-away status in `proposal_status_enum`, confirmed against the dashboard's own display-status mapping, not guessed); and which tiers are actually capped (only `free` — `pay_per_proposal` and `agency` are both explicitly uncapped by proposal count in the same pricing copy).

**`lib/proposalLimits.ts`** (new) — `checkActiveProposalLimit(supabase, accountId)`, the one shared function called from both creation paths (not duplicated logic): reads `plan_tier`, returns `{allowed:true}` immediately for any non-`free` tier, otherwise counts non-`ARCHIVED` proposals and blocks at `FREE_TIER_ACTIVE_PROPOSAL_LIMIT = 1`. Same shape as the existing `connectDomain()` plan-tier check in `app/dashboard/settings/actions.ts`, not invented from scratch. Wired into `app/api/proposals/route.ts` (returns `403` with `{error: reason}` — the exact shape `NewProposalClient.tsx` already expected) and `duplicateProposalAsDraft()` (throws `Error(reason)` — the exact shape `DashboardClient.tsx`'s existing catch already surfaces as a toast). No DB-level constraint added; a genuine concurrent double-create race is unhandled, same looseness every other plan-tier check in this app already accepts.

**Sahil's own real account was a real, materially-affecting consequence, flagged and confirmed before building, not assumed:** it was `plan_tier: 'free'` with 2 active proposals already — it would have been blocked from creating anything new the moment this shipped. Explicitly confirmed to bump it to `plan_tier: 'agency'` in the local DB (one-time data change, not code) rather than leaving it on `free` to hit the same restriction himself.

`npx tsc --noEmit` clean. `git diff --stat`: `lib/proposalLimits.ts` new; `app/api/proposals/route.ts` (+6), `app/dashboard/actions.ts` (+4) — nothing else. **All verification done against real accounts and real UI flows, not mocks:** the free-tier test account (`test@invictus.app`) had accumulated 21 stale rows from this session's own prior dev-proof runs — trimmed to exactly 1 active proposal via the existing `deleteProposal` UI action (not a raw DB edit) before testing. (1) Attempting a second proposal through the real "Confirm before we build" UI produced a real, logged `403 Forbidden` and a clear, non-silent in-app error — "Couldn't save the proposal — Free plan is limited to 1 active proposal..." — with the generated content preserved, not lost. (2) The existing `agency`-tier account and the existing `pay_per_proposal`-tier account (both already in local data, no new accounts created) confirmed `{allowed: true}` via the real function against their real current data, including the agency account already having 1 active proposal — unaffected either way. (3) Archiving the free-tier account's one active proposal (via the real "Unpublish" action) and retrying produced a real, logged `200 OK` — the "active" definition genuinely un-blocks, not just blocks. (4) `duplicateProposalAsDraft` independently hit the identical block from the same blocked-again state, surfaced as a real toast — confirmed as a second, independently-gated path, not just the one route. |
| Sep 9, 2026 | **Timeline phase duration — real format validation, closing the `CURRENT_STATE_SNAPSHOT.md` §7 item** | 1) A structured number+unit input (dropdown/stepper), constraining the shape structurally. 2) Free text kept, with non-blocking inline validation feedback on blur, reusing the parser's own function directly. **Chosen: (2).** | **Closed.** | `CURRENT_STATE_SNAPSHOT.md` §7 logged this as "a basic character-length cap... not real format validation... flagged as a bigger UX decision, deliberately not built out further." Now load-bearing: `lib/ai/mapProposalToDealFacts.ts` (built the same night) silently falls back to `validUntil`+30 days whenever any phase's duration fails its exact pattern — free text meant most real proposals would keep landing on that fallback.

**Why (2), not (1), stated plainly:** a structured control makes an invalid value impossible while typing, but it's the first control of its kind in this editor (every other field in every block — `PackagesBlock`, `AttachmentsBlock`, this one — is plain text/textarea on the same whole-document autosave), and it forces an immediate decision about how to *display* the real legacy non-conforming values the earlier audit already found (`"Monthly"`, `""`) inside a widget that can't represent them. Free text + inline feedback keeps the field exactly as every sibling field behaves, needs no legacy-display mode, and — critically, since `ProposalEditor.tsx`'s autosave is whole-document and debounced, not per-field — validating only as non-blocking visual feedback avoids a real problem a hard save-block would cause: `addPhase()` starts every new phase with `duration: ''`, which is itself non-conforming, so blocking the whole document's save over one freshly-added, still-blank field would misfire on a completely normal mid-edit state.

**Reused, not restated:** exported `parseDurationDays` from `lib/ai/mapProposalToDealFacts.ts` (previously private) and imported it directly into `TimelineBlock.tsx` — the literal same function the due-date computation itself calls, not a re-implemented copy of the regex, so the two can never drift apart.

**Phase identity, confirmed before building, not assumed (per a question raised before this plan was approved):** `TimelineBlock.tsx` supports add and delete, no reordering. `TimelinePhase` has no `id` field — every existing per-phase reference in the file (`updatePhase(index, ...)`, `pendingDelete: number | null`, even React's own list `key={idx}`) is already positional. So the new `touched: Set<number>` state follows an existing pattern, not a new one — but it's the first *long-lived* index-keyed state in the file (existing ones are single-shot), which is the one place a deletion's index-shift could produce a stale reference. Closed before shipping: `confirmRemovePhase` now reindexes `touched` the same way `timeline` itself shifts (drop the removed index, decrement every index above it by one).

`npx tsc --noEmit` clean. `git diff --stat`: `lib/ai/mapProposalToDealFacts.ts` (+1, export only), `components/editor/TimelineBlock.tsx` (+28/-3) — nothing else.

**Verification, against real data, not fixtures:** (1) Edited a real draft proposal's timeline live in the editor — set a phase to `"Monthly"`, confirmed the hint appeared on blur, confirmed the value persisted to the DB unchanged (`["Monthly", "5 days", "3 days"]`) and the document's `PATCH` autosave still returned real `200 OK`s throughout — old/non-conforming data is never blocked, exactly as designed. (2) Directly proved the reindex mitigation, not just trusted it: made two adjacent phases invalid (`"Monthly"`, `"Weekly"`) and touched both, deleted the first, and confirmed the second phase's hint correctly survived at its new index rather than silently disappearing — the exact failure mode identified during planning, shown not to occur. (3) Set the same proposal's phases to clean durations (`"1 week"`, `"3 days"`, real `dateIssued` 9/9/2026, real `validUntil` 9/23/2026) and called the real `mapProposalToDealFacts` function directly against the real, just-edited DB row: computed `due_date` was `2026-09-19`, matching the hand-computed parsed-and-summed expectation exactly (September 19) and clearly not the fallback expectation (October 23) — unambiguous, not a coincidental match. **Deviation from the plan's own verification step, disclosed rather than silently substituted:** the plan asked to run this through the real `beta-ai-page` HTTP route, but the only account with real non-conforming legacy data reachable without a credential reset (`test@invictus.app`) isn't in `BETA_AI_ENGINE_ACCOUNT_IDS`, and editing `.env.local` to add it was itself blocked by the environment's own permission classifier as a sensitive-config change — correctly declined rather than worked around. Used the identical underlying function directly against the real row instead, which is the entire piece of logic in question; the HTTP route around it (auth, flag check, generation pipeline) was already proven end-to-end in the prior integration task and is unchanged by this one. |
