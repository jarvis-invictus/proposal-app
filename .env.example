# Environment variables required to run Marg.
# Copy this file to .env.local for local dev, and set the same names in the Vercel project's
# Environment Variables settings for production. Values below are dummy placeholders — replace
# every one with the real value from the corresponding dashboard.
#
# Everything under "Not yet integrated" documents what production will eventually need, per
# docs/MVP_LAUNCH_PLAN.md, even though no code reads those vars yet — so this file stays the
# single checklist for configuring Vercel instead of two different lists living in two places.

# ---------------------------------------------------------------------------
# Supabase — database, auth, storage. Required.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=REPLACE_WITH_YOUR_SUPABASE_PROJECT_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_YOUR_SUPABASE_ANON_KEY
# Server-only — never expose to the client. Bypasses RLS. Validated by env.ts, so an unset value
# fails fast at boot with a clear error instead of crashing per-request deep in a service-role
# code path (the public proposal page, settings, publish/accept/webhook routes, etc.).
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_YOUR_SUPABASE_SERVICE_ROLE_KEY

# Google sign-in ("Continue with Google") needs no env vars here — it's configured entirely in
# the Supabase dashboard (Authentication → Providers → Google, with your Google OAuth client
# ID/secret) plus the callback URL registered in Google Cloud Console
# (https://<project>.supabase.co/auth/v1/callback). The app side is just
# app/auth/callback/route.ts, which reads the same Supabase project above.

# ---------------------------------------------------------------------------
# OpenAI — proposal generation (generateObject) and the intake chat (streamText). Required.
# ---------------------------------------------------------------------------
OPENAI_API_KEY=REPLACE_WITH_YOUR_OPENAI_API_KEY

# ---------------------------------------------------------------------------
# Firecrawl — scrapes a client's website to read their brand (colors/fonts/logo). Required.
# ---------------------------------------------------------------------------
FIRECRAWL_API_KEY=REPLACE_WITH_YOUR_FIRECRAWL_API_KEY

# ---------------------------------------------------------------------------
# Resend — transactional email (team invites, "your proposal was signed" notifications).
# Free tier is enough at current scale. If unset, no email is actually sent — instead the exact
# email is logged to the console ("Mock Email Sent to ...: ... - Link: ..."), so invite/signing
# flows are still fully testable locally without a Resend account.
# ---------------------------------------------------------------------------
RESEND_API_KEY=REPLACE_WITH_YOUR_RESEND_API_KEY

# ---------------------------------------------------------------------------
# Upstash Redis — rate limiting on the AI endpoints (/api/generate-proposal, /api/chat).
# Free tier is enough at current scale. If these are unset, rate limiting is bypassed and a
# warning is logged in development — the app still runs, this is not a hard requirement locally.
# ---------------------------------------------------------------------------
UPSTASH_REDIS_REST_URL=REPLACE_WITH_YOUR_UPSTASH_REDIS_REST_URL
UPSTASH_REDIS_REST_TOKEN=REPLACE_WITH_YOUR_UPSTASH_REDIS_REST_TOKEN

# ---------------------------------------------------------------------------
# Sentry — error tracking. Free tier is enough at current scale. If NEXT_PUBLIC_SENTRY_DSN is
# unset, Sentry is disabled entirely (client, server, and edge) and the build/app run normally.
# SENTRY_ORG/PROJECT/AUTH_TOKEN are only needed for uploading source maps at build time — safe
# to leave unset locally.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SENTRY_DSN=REPLACE_WITH_YOUR_SENTRY_DSN
SENTRY_ORG=REPLACE_WITH_YOUR_SENTRY_ORG_SLUG
SENTRY_PROJECT=REPLACE_WITH_YOUR_SENTRY_PROJECT_SLUG
SENTRY_AUTH_TOKEN=REPLACE_WITH_YOUR_SENTRY_AUTH_TOKEN

# ---------------------------------------------------------------------------
# Stripe — Marg's own subscription billing (§3.1a of docs/MVP_LAUNCH_PLAN.md). Required for
# /api/billing/checkout and /api/webhooks/stripe to actually work; both degrade to a clean,
# expected error (not a crash) when unset — see those routes' own comments. Global-first/USD
# per the launch plan; Razorpay was the earlier, now-superseded decision.
#
# STRIPE_PRICE_ID_* are the Stripe Price object ids for the two paid plans, created in the
# Stripe Dashboard (Products) — the actual amount/currency lives there, not in this codebase.
# ---------------------------------------------------------------------------
STRIPE_SECRET_KEY=REPLACE_WITH_YOUR_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=REPLACE_WITH_YOUR_STRIPE_WEBHOOK_SECRET
STRIPE_PRICE_ID_PAY_PER_PROPOSAL=REPLACE_WITH_YOUR_STRIPE_PRICE_ID
STRIPE_PRICE_ID_AGENCY=REPLACE_WITH_YOUR_STRIPE_PRICE_ID
# Not read by any code yet — kept for a future Stripe.js use (e.g. a billing-portal button).
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=REPLACE_WITH_YOUR_STRIPE_PUBLISHABLE_KEY

# ---------------------------------------------------------------------------
# Branded per-account links (yourstudio.<this>/p/<slug>) instead of the shared domain — free on
# every plan, no DNS/CNAME work for the account owner. If unset, every public link falls back to
# the current origin, which is correct behavior locally and until this is actually set up. Two
# things need to be true before a branded link resolves in production: this var points at a
# domain Sahil owns, AND a wildcard domain (*.<this>) is added to the Vercel project. Neither
# has happened yet — setting this alone only reserves subdomain names, it doesn't make them live.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_ROOT_DOMAIN=REPLACE_WITH_YOUR_ROOT_DOMAIN

# ---------------------------------------------------------------------------
# Skydo — cross-border payment receipt for Marg's own (India-based) users getting paid by
# international clients (§3.1b, explicitly post-MVP, not yet integrated in code). Not a Stripe
# alternative — a different job (see §3.1's writeup for why the two aren't interchangeable).
# ---------------------------------------------------------------------------
SKYDO_API_KEY=REPLACE_WITH_YOUR_SKYDO_API_KEY
